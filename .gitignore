venv/
.venv/
.env
logs/
__pycache__/
**/__pycache__/
.pytest_cache/
.DS_Store

# Local backtest artifacts (committed example CSVs are kept).
data/raw/
data/*.parquet
data/cache/

# Research workbench outputs - never part of a submission.
research/cache/
research/results/
*.tearsheet.html
tearsheet*.html
trades*.csv
indicators*.csv

# Private planning docs live outside the repo (../soai-2026-private/).
PLAN.md

# Minute-bar CSVs are cached locally only (research/cache/*.parquet); the
# harness uses hourly bars, so committing minute data stores 60x what it needs.
data/*_1m_spot.csv
!data/EXAMPLE_1m_spot.csv
